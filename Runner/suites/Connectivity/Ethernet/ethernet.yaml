metadata:
    format: Lava-Test Test Definition 1.0
    name: AUSanity
    description: "Ethernet test"
    maintainer:
        - vnarapar@qti.qualcomm.com
    os:
        - openembedded
    scope:
        - functional
    devices:
        - rb3gen2

run:
    steps:
        - cd Runner
        - $PWD/suites/Connectivity/Ethernet/run.sh || true
        - $PWD/utils/send-to-lava.sh $PWD/suites/Connectivity/Ethernet.res || true

